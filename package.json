{
    "name": "pokedemo",
    "version": "0.0.1",
    "description": "",
    "scripts": {
        "build:packages": "pnpm run --filter=./packages/* build",
        "build:backend": "pnpm run --dir apps/backend build"
    },
    "license": "ISC",
    "private": true,
    "type": "module",
    "packageManager": "pnpm@8.6.11",
    "engines": {
        "pnpm": "^8.0.0"
    },
    "devDependencies": {
        "typescript": "^5.0.2"
    }
}
